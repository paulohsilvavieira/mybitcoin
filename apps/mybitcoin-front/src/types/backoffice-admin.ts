/** Perfil do administrador logado no backoffice — mock de UI, sem endpoint real ainda. */
export interface AdminProfile {
  nome: string
  email: string
  cargo: string
  twoFactorEnabled: boolean
}
